"use client";
import SearchBox from './SearchBox';

export default function SearchPageList({ query, category }: { query: string; category: string }) {

  return (
    <div>
      <ul className="w-full mt-4">
      </ul>
    </div>
  );
}